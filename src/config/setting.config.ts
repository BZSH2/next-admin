export const settingConfig = {
  /**
   * 系统标题
   */
  title: 'Next Admin',
  /**
   * 侧边栏默认是否折叠
   */
  collapsed: false,
  /**
   * 客户端持久化（cookie / storage）统一键名前缀
   */
  cacheKeyPrefix: '',
  /**
   * 默认主题色
   */
  primaryColor: '#1677ff' as `#${string}`,
}
